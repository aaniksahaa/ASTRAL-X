#!/usr/bin/env bash
# ASTRAL-X developer launcher: forwards to scripts/run.sh (build + run from source).
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${ROOT}/scripts/run.sh" "$@"
